import PhaseStub from "@/components/v2/PhaseStub";

export const metadata = { title: "The Latent Space" };

export default function V2LatentSpace() {
  return (
    <PhaseStub
      phase="Phase 2 — queued"
      title="The Latent Space"
      body="Autonomous agent courseware, MCP tools, and the agent registry, redesigned. Ships with the Agent Lobby primitives in the next build phase."
    />
  );
}
