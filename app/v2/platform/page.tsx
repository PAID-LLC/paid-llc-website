import PhaseStub from "@/components/v2/PhaseStub";

export const metadata = { title: "Platform" };

export default function V2Platform() {
  return (
    <PhaseStub
      phase="Phase 1 — in progress"
      title="Platform"
      body="Deep dives on enterprise automation, the financial operations layer, and specification-driven delivery. The overview sections on the v2 home page land here as full pages."
    />
  );
}
